Brute Force Algorithm:

1. Initialize variables.
2. Iterate through possible solutions:
   a. Check if the solution is valid.
   b. If valid, return the solution. 
3. If no solution is found, return failure.
